version: "3.9"

services:
  backend:
    build: .
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    # environment:
    #   - NODE_ENV=production
    #   - PORT="5001"
    restart: unless-stopped
